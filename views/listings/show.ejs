<% layout("/layout/boilerplate") %>

<body>
  
  <div class="show-card" style="width: 18rem">
    <h3>List :</h3>
    <img src="<%= list.image %>" class="card-img-top" alt="<%= list.title %>" />
    <div class="show-card-body">
      <h5 class="show-card-title"><%= list.title %></h5>
      <p class="show-card-text">
        <%= list.description %> <br />
        &#8377; <%= list.price.toLocaleString("en-IN") %> / night <br />
        <%= list.location %><br />
        <%= list.country %>
      </p>
      <div class="btns">
        <button class="btn btn-warning"><a style="color:black;text-decoration: none;" href="/listings/<%= list._id %>/edit">Edit</a></button><br /><br />
      <form
        method="post"
        action="/listings/<%= list._id %>/delete?_method=DELETE"
      >
        <button class="btn btn-danger">Delete</button>
      </form>
      </div>
    </div>
  </div>

  <!-- <ul>
    <li><%= list.title %></li>
    <li><%= list.description %></li>
    <li>&#8377; <%= list.price.toLocaleString("en-IN") %></li>
    <li><%= list.location %></li>
    <li><%= list.country %></li>
  </ul> -->
</body>
